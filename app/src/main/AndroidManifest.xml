<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClassroomConnect">

        <activity
            android:name=".ui.auth.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.library.PersonalLibraryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.classroom.DiscussionForum"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.classroom.ClassDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.teacher.TeacherActivity"
            android:exported="false" />

        <activity
            android:name=".ui.student.StudentActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.SignUp"
            android:exported="false" />

        <activity
            android:name=".ui.auth.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.quiz.QuizActivity"
            android:exported="false" />

        <activity
            android:name=".ui.quiz.QuizHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.quiz.ReviewQuizActivity"
            android:exported="false" />

        <service
            android:name=".ui.notifications.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>